version: 2.2
condition: if [ ! -f "/%artifacts_output_directory%/system/hidden_directories.txt" ]; then true; else false; fi
output_directory: /system
artifacts:
  -
    description: List all hidden directories outside of user home directory.
    supported_os: [all]
    collector: find
    path: /
    name_pattern: [".*"]
    exclude_path_pattern: ["/root", "/home", "/export/home", "/Users"]
    exclude_file_system: [proc, procfs]
    file_type: [d]
    output_file: hidden_directories.txt
  