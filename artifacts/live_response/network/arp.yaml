description: Run and collect arp command output.
version: 1.0
artifacts:
  -
    description: Collect the kernel's IPv4 network neighbour cache.
    collector: command
    supported_os: [aix, freebsd, linux, macos, netbsd, netscaler, openbsd, solaris]
    command: arp -a
    output_file: arp_-a.txt