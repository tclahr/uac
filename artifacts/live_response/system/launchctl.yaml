description: Run and collect launchctl command output.
version: 1.0
artifacts:
  -
    description: Display all loaded and unloaded jobs into launchd.
    collector: command
    supported_os: [macos]
    command: launchctl list
    output_file: launchctl_list.txt
