description: Run and collect pcidump command output.
version: 1.0
artifacts:
  -
    description: Display PCI device data.
    collector: command
    supported_os: [openbsd]
    command: pcidump -v
    output_file: pcidump_-v.txt
  
